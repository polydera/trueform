---
title: Introduction
description: Welcome to trueform — real-time geometric processing for C++.
navigation:
  icon: i-lucide-house
---

`trueform` is a C++ library for real-time geometric processing, built on the principles of composable views and inline policy injection. It operates directly on your *plain-old-data*, by providing semantic views that wrap it with geometric meaning.

From individual primitives to structured ranges, from metadata injection to spatial and topological processing — every operation happens directly on your data; enriched with semantics, without architectural changes.

The library integrates directly at the call site: no boilerplate, no architectural rewrites, no heavyweight setup. It acts as a lightweight, expressive layer over your existing data. Like C++ ranges or lambdas, it lets you build rich, semantic geometry inline, without sacrificing performance or control.


::card-group
  :::card
  ---
  icon: i-lucide-rocket
  title: Getting Started
  to: /getting-started/installation
  ---
  Requirements and installation via CMake `FetchContent`.
  :::

  :::card
  ---
  icon: i-lucide-box
  title: Tutorial
  to: /modules/core
  ---
  Core primitives, buffers, policies, and parallel algorithms.
  :::

  :::card
  ---
  icon: i-lucide-chart-bar-stacked 
  target: _blank
  title: Real-Time Performance
  to: /benchmarks
  ---
  Benchmarked against VTK, CGAL, nanoflann, and IGL.
  :::

  :::card
  ---
  icon: i-lucide-file-text
  title: Examples
  to: /examples/overview
  ---
  Extensive examples and VTK integration.
  :::
::

## Key Features

- **Zero-copy views**: work directly on your data layout with semantic geometric wrappers.
- **Composable policies**: enrich primitives and ranges with `id`, `normal`, `state` via tag/zip.
- **Spatial acceleration**: `tf::tree` for k-NN, neighbor search, ray casting, and broad-phase queries.
- **Topology**: connectivity structures (face membership, vertex link, manifold edge link), path finding, planar embeddings.
- **Intersections**: mesh-mesh curves, self-intersections, scalar field isocontours, segment intersections.
- **Cutting**: embed curves as edges via face splitting; boolean operations (union, intersection, difference); planar arrangements.
- **Data management**: efficient flat buffers with direct memory access; reindexing and filtering with automatic referential integrity; cleaning operations for duplicates and degenerates.
- **Parallel algorithms**: generic data generation, blocked reduction, parallel apply/transform/copy built on Intel TBB.

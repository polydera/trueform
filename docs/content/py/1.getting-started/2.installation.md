---
title: Installation
description: Install trueform via pip.
navigation:
  icon: i-lucide-download
---

## Requirements

- **Python 3.8** or later

## pip install

```bash
pip install trueform
```

This installs:
- **Python bindings** — full trueform API for Python
- **C++ headers** — header-only library for C++ projects

::tip{icon="i-lucide-code"}
For C++ CMake integration from the pip package, see [C++ Installation](/cpp/getting-started/installation#pip-install).
::

## Building from Source

For development or unsupported platforms:

```bash
git clone https://github.com/xlabmedical/trueform.git
cd trueform

cmake -B build -DTF_BUILD_PYTHON=ON
cmake --build build --target python
```

Install the built package:

```bash
pip install ./build/python
```

## Verify

```python
import trueform as tf

print(tf.__version__)
```

examples/src/alignment.cpp:123 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
examples/src/alignment.cpp:306 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/arrangement/planes/make_plane_weld_entrant_tables.hpp:73 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/clean/index_map/segments.hpp:76 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/core/algorithm/parallel_copy_by_map_with_nones.hpp:36 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/core/centroid.hpp:124 10.1: local constexpr 'Dims' referenced in lambda
include/trueform/csg/graph/compute_bundle_tag_index.hpp:123 10.5: default-capture generic lambda decomposes 't' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/csg/graph/compute_bundle_tag_index.hpp:84 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/csg/graph/evaluate_per_domain.hpp:38 10.5: default-capture generic lambda decomposes 't' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/csg/graph/make_csg_domain_partition.hpp:132 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/csg/graph/make_csg_partition.hpp:86 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_plane.hpp:181 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_plane.hpp:193 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_plane.hpp:71 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_plane.hpp:82 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_point.hpp:159 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_point.hpp:170 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_point.hpp:64 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/alignment/fit_knn_alignment_point_to_point.hpp:74 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/compute_shape_index.hpp:55 10.1: local constexpr 'two_over_pi' referenced in lambda
include/trueform/geometry/laplacian_smoothed.hpp:81 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/make_sharp_edges.hpp:50 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/geometry/taubin_smoothed.hpp:65 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/intersect/graph/build_plane_csr.hpp:42 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/intersect/graph/build_plane_csr.hpp:60 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/intersect/graph/name_plane_carriers.hpp:120 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/intersect/graph/state_uncut_entrants.hpp:83 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/intersect/identity/form_point_identities.hpp:94 10.5: default-capture generic lambda decomposes 'bufs' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/intersect/segments/dedup_segment_vertex_points.hpp:81 10.5: default-capture generic lambda decomposes 'bufs' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/io/obj/make_obj_vertex_triplets.hpp:72 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/io/obj/make_obj_vertex_triplets.hpp:80 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/io/obj/read_complete_obj.hpp:153 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/io/obj/resolve_obj_labels.hpp:45 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/iso/cut/build_iso_cuts.hpp:67 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/iso/cut/gather_iso_band_triangles.hpp:75 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/iso/cut/make_surface_scalar_labels.hpp:48 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/reindex/make_dynamic.hpp:92 10.1: local constexpr 'ngon' referenced in lambda
include/trueform/reindex/split_into_domains.hpp:113 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/reindex/split_into_domains.hpp:125 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/spatial/tree/build_aabb_nodes.hpp:137 10.1: local constexpr 'n_buckets' referenced in lambda
include/trueform/spatial/tree/build_aabb_nodes.hpp:451 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/spatial/tree/build_aabb_nodes.hpp:459 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/spatial/tree/build_aabb_nodes.hpp:483 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/components/finder.hpp:36 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/domains/canonicalize_nm_edges.hpp:120 10.5: default-capture generic lambda decomposes 't' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/domains/emit_boundary_merges.hpp:46 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/domains/filter_face_blocks.hpp:44 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/domains/filter_face_blocks.hpp:59 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/edge_path_connector.hpp:91 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/is_closed.hpp:41 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/non_manifold_edges.hpp:45 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/non_simple_edges.hpp:47 10.5: default-capture generic lambda decomposes 'buffers' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/path_connector.hpp:74 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/sidedness/aggregate_votes.hpp:105 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/sidedness/aggregate_votes.hpp:138 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/sidedness/emit_votes.hpp:72 10.5: default-capture generic lambda decomposes 'buffers' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
include/trueform/topology/vertex_link.hpp:123 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/examples/laplacian_smoothing.cpp:310 10.5: default-capture generic lambda decomposes 'tup' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/examples/patches.cpp:235 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/examples/patches.cpp:279 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/examples/patches.cpp:662 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/examples/patches.cpp:698 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/src/functions/neighbor_search_batch.cpp:34 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/src/functions/neighbor_search_batch.cpp:52 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/src/functions/neighbor_search_batch.cpp:74 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
vtk/src/functions/neighbor_search_batch.cpp:94 10.5: default-capture generic lambda decomposes 'pair' -- MSVC loses the enclosing scope after the binding; list what the body reads in the capture list
